/*
process.stdin.on('data', (data) => {
    console.log(data.toString());  // Recevoir les datas sous forme de data plutôt que de la recevoir sous forme de Buffer
})

process.stdin.on('end', () => {
    console.log('stdin closed');
});
*/
