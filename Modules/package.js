/*
const colors = require ('colors');

console.log('Hello'.trap.bgCyan);
console.log(colors.america("America Fuck Yeah !!"));
*/

/*
const { Image } = require('image-js');

execute().catch(console.error);

async function execute() {
  let image = await Image.load('cat.png');
  let grey = image
    .grey() 
    .resize({ width: 200, height: 200 })
    .rotate(270); 
  return grey.save('cat1.png');
}
*/

/*
const { Image } = require('image-js');

execute().catch(console.error);

async function execute() {
  let image = await Image.load('Romain.png');
  let grey = image
    .invert(2)
    .gaussianFilter(4)
    .sobelFilter(5)
  return grey.save('Rom1.png');
}
*/

